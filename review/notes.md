primeira coisa para começar um projeto node.js
dar o comando npm init -y para iniciar o servidor

para iniciar o programa use o npm start


primeiros comandos:

const express = require("express")
const PORT = 3000
const app = express